import XCTest
@testable import GistHubMarkdownEditor

final class GistHubMarkdownEditorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(GistHubMarkdownEditor().text, "Hello, World!")
    }
}
